<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/main.css">
    <title>My Chats</title>
</head>
<body>
    <header>
        <h1>My Chats</h1>
        <%- include('../nav.ejs') %>
    </header>
    <main>
        <ul class="chat-list">
            <% if (chats && chats.length > 0) { %>
                <% chats.forEach(chat => { %>
                    <li>
                        <a href="/api/chat/detail/<%= chat._id %>">
                            Chat about "<%= chat.postTitle %>"
                            on <%= new Date(chat.date).toLocaleDateString() %>
                        </a>
                    </li>
                <% }) %>
            <% } else { %>
                <li>No chats available.</li>
            <% } %>
        </ul>
    </main>
    <footer>
        <p>© 2024 Our Marketplace</p>
    </footer>
</body>
</html>
